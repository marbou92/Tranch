# Addon placeholder
# This folder holds the GUT (Godot Unit Test) addon, installed by:
#   bash scripts/install_gut.sh
# CI installs it automatically before running tests.
# Do NOT commit GUT itself — it's downloaded on demand to keep the repo small.
